# Web application with JS/node

Experimenting with some concepts usually provided by frameworks.

The end goal is to make a little blog post as an exercise.

This is in no way a proper implementation, and may take a long time while unfinished

## To run it
```bash
$    npm install
$    node_modules/bin/tsc --outdir ./build
$    npm run serve
```

## Explore what exists now on links (after running the server):
* http://localhost:9090/
* http://localhost:9090/#home
* http://localhost:9090/#admin

TODO:
* [ ] Add application pages (to navigate along the blogposts)
* [ ] Add user Authorization/Authentication
* [ ] Fix inter-page routing
* [ ] Bundling for the static pages
* [ ] Add configuration files
* [ ] Better logging
